import * as alt from 'alt-client'
import * as native from 'natives';

let panel;
//panel.isVisible = false;
/*
alt.onServer('base:openSkinMenu', () => {
    panel = new alt.WebView("http://resource/client/browser/html/skin_panel/index.html");
    panel.isVisible = true
    panel.focus()
    alt.showCursor(true);
});


panel.on("close", () => {
    panel.visible = false;
    alt.showCursor(false)
    alt.log("DASDBAHBDH")
});
*/
